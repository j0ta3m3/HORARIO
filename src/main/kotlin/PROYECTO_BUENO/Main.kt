package PROYECTO_BUENO

fun main() {

    /*var horario1 = Horario()

    println("Introduzca un horario")
    var entrada = readLine().toString()
    Horario().formateo(entrada)*/


    /* var confirm=false

        var horario1 = Horario()

    while(confirm!=true) {
        for(i in 0..Horario().Horario.size) {
            println("Introduzca un horario")
            var entrada = readLine().toString()
            var hola:List<String> = Horario().formateo(entrada)
            Horario().Horario[i]= hola[i]
            //Horario().Horario.size==
            if (entrada.isEmpty()) {
                confirm = true

            }
        }
    }

    println(Horario().Horario.toString())*/

}
